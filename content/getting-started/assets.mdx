import { FileTreeLabel } from '@/components/FileTreeLabel'
import { Callout, FileTree } from 'nextra/components'

# Assets

Since **v2.0**, WP Bones compiles your plugin assets with a single `webpack.config.js` built on top
of [`@wordpress/scripts`](https://www.npmjs.com/package/@wordpress/scripts). The config
**auto-discovers** every file under `resources/assets/` — you drop a file in the right folder and
`yarn build` picks it up.

Supported formats:

- **Styles** — CSS, LESS, Sass
- **Scripts** — JavaScript, TypeScript
- **React apps** — `.jsx`, `.tsx` (folder-based or single-file)

## Install

```sh copy
yarn install
```

## Build

One-shot production build:

```sh copy
yarn build
```

Watch mode during development:

```sh copy
yarn dev
```

## Folder layout

Source tree under `resources/assets/`:

<FileTree>
  <FileTree.Folder name="resources/assets" defaultOpen>
    <FileTree.Folder name="apps" defaultOpen>
      <FileTree.Folder name="dashboard" defaultOpen>
        <FileTree.File name={<FileTreeLabel type="file" name="index.tsx">React + TS (folder-based)</FileTreeLabel>} />
      </FileTree.Folder>
      <FileTree.File name={<FileTreeLabel type="file" name="widget.tsx">React + TS (single-file)</FileTreeLabel>} />
    </FileTree.Folder>
    <FileTree.Folder name="css" defaultOpen>
      <FileTree.File name={<FileTreeLabel type="file" name="admin.scss">Sass</FileTreeLabel>} />
      <FileTree.File name={<FileTreeLabel type="file" name="frontend.less">Less</FileTreeLabel>} />
      <FileTree.File name={<FileTreeLabel type="file" name="prism.css">CSS</FileTreeLabel>} />
    </FileTree.Folder>
    <FileTree.Folder name="js" defaultOpen>
      <FileTree.File name={<FileTreeLabel type="file" name="helpers.ts">TypeScript</FileTreeLabel>} />
      <FileTree.File name={<FileTreeLabel type="file" name="prism.js">JavaScript</FileTreeLabel>} />
    </FileTree.Folder>
  </FileTree.Folder>
</FileTree>

Compiled output under `public/`:

<FileTree>
  <FileTree.Folder name="public" defaultOpen>
    <FileTree.Folder name="apps" defaultOpen>
      <FileTree.File name={<FileTreeLabel type="file" name="dashboard.js">React bundle</FileTreeLabel>} />
      <FileTree.File name={<FileTreeLabel type="file" name="dashboard.asset.php">Deps manifest</FileTreeLabel>} />
      <FileTree.File name={<FileTreeLabel type="file" name="widget.js">React bundle</FileTreeLabel>} />
    </FileTree.Folder>
    <FileTree.Folder name="css" defaultOpen>
      <FileTree.File name={<FileTreeLabel type="file" name="admin.css">CSS</FileTreeLabel>} />
      <FileTree.File name={<FileTreeLabel type="file" name="frontend.css">CSS</FileTreeLabel>} />
      <FileTree.File name={<FileTreeLabel type="file" name="prism.css">CSS</FileTreeLabel>} />
    </FileTree.Folder>
    <FileTree.Folder name="js" defaultOpen>
      <FileTree.File name={<FileTreeLabel type="file" name="helpers.js">JavaScript</FileTreeLabel>} />
      <FileTree.File name={<FileTreeLabel type="file" name="prism.js">JavaScript</FileTreeLabel>} />
    </FileTree.Folder>
  </FileTree.Folder>
</FileTree>

## Commands

| Command | What it does |
|---------|--------------|
| `yarn dev` | Webpack in watch mode |
| `yarn build` | One-shot production build |
| `yarn test` | Jest via `wp-scripts test-unit-js` |
| `yarn format` | Prettier on `resources/` |
| `yarn lint` | ESLint on `resources/` |

## Adding a new React app

```sh copy
php bones make:app dashboard
```

Scaffolds `resources/assets/apps/dashboard/index.tsx` with a React 18 + TypeScript template. No
`package.json` edits required — webpack auto-discovers the new entry.

See also [`webpack.config.js`](/docs/core-plugin-files/webpack-config) and
[`package.json`](/docs/core-plugin-files/package-json).

<Callout type="info">
Coming from a v1.x gulp-based plugin? Run `php bones migrate:to-v2`. See the full
[Migrating to v2](/docs/migrating-to-v2) guide.
</Callout>
